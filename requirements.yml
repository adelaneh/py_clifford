name: py_clifford_dev
channels:
  - conda-forge
  - adelaneh
  - defaults
  - intel
dependencies:
    - setuptools
    - pandas
    - nose
    - numpy
    - matplotlib
    - tensorflow==1.15
    - tqdm
